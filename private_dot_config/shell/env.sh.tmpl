# Generated by chezmoi from .chezmoidata/shell.toml -- edits here are lost.
{{ range $k, $v := .shell.env }}
export {{ $k }}="{{ $v }}"
{{- end }}
{{ range .shell.path }}
case ":$PATH:" in
    *":$HOME/{{ . }}:"*) ;;
    *) PATH="$HOME/{{ . }}:$PATH" ;;
esac
{{- end }}
export PATH
